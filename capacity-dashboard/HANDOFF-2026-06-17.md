# 継続ハンドオフ 2026-06-17（capacity-dashboard SPA）

> このファイルは「次の人（未来の自分含む）がコンテキスト無しで続きをやる」ための継続メモ。
> 正本の全体runbook/アーキテクチャは [HANDOFF.md](HANDOFF.md)。本書はその後の差分＋進行中の論点。

---

## 0. 前提（混同注意・重要）

- **実ユーザー = 森田**（Vikunja `users.id=7`、username "森田"、email **ikimonogakaru@gmail.com**）。ログインはこのアカウント。`capdemo`(id1) は**デモ/UIスモーク用**（担当タスク0なので「自分の担当」は空）。
- 旧シード `morita`(id2) は **2026-06-17 に id7(森田) へ統合済み**（assignees/plans/entries/unavailability/recurrences・users_projects）。詳細は memory `capacity-dashboard-user-identity`。
- **呼称階層**: ワークスペース(=Vikunja project) ＞ **プロジェクト(=親タスク, related_tasks)** ＞ タスク。「プロジェクト」はWSではなく**親タスク**。
- 配信: `http://leo:7010/app/`（no-store配信だが**ESMはフルリロードで反映**＝Playwright検証時は `location.reload()`）。バックエンドAPI: `http://leo:7005`（Vikunjaフォーク、`pm-vikunja` コンテナ）。日次バックアップ: `~/backups/taskstation/`。
- **検証はブラウザ目視必須**（ユーザー強い要望）。DOMチェックだけでなくスクショで見る。

---

## 1. 今セッションでやったこと（push済み or ローカルコミット）

UI/UX大改修。主なもの：

- **絵文字→SVGアイコン全面統一**（`lib/icons.js` 新設）。トップバー（タイマー/ベル/検索）含む。
- **画面整理**: 週プラン/週日別負荷/月次空きを削除。レビュー編集をSPAモーダル化＋承認Undoトースト。
- **ホーム再構成**: 縦積み（KPI→やること→稼働予定→稼働プラン→月間ガント）＋セクション折りたたみ（`ts.home.fold.<uid>`）＋埋め込み可変高＋ガント編集可。「やること」バケット（未アサイン/今日/明日〜7日）。
- **ガント**: タスク行モード削除（プロジェクト別/人別の2モード）。人別を親(プロジェクト)で階層表示＋ツリー線（色=プロジェクト色）＋未アサイングループ＋全展開/全折りたたみ＋容量線(capacityOn)。
- **タスク一覧（旧「一覧」→「タスク一覧」）**: 表/階層トグル（旧アウトライン統合）、階層DnD親付け替え、複数選択＋一括操作、検索（右上）、フィルタ整頓、定例業務帯、**スマートリスト風プリセットタブ**（すべて/未整理/今日/…/完了・件数バッジ・`lib/smartlist.js`の`taskMatches`共有）、**プロジェクト列（親タスク名）**、完了表示既定=「非表示（今日の完了は表示）」、見積ポップアップを**pendingモデル**（適用まで保存しない・[クリア｜キャンセル｜適用]）。
- **クイック追加**: 構文チップ化（適用/テキスト化トグル）＋💡ヘルプ。
- **スマートリスト**: 左レール→上部横タブ化。「インボックス」→「未整理」（定義＝未完了 かつ 担当者or期限が未設定。`unsorted`フラグ）。各ビューに`desc`表示。
- **設定**: 個人設定/チーム設定の2メニューに分割。**分類（ラベル）マスタ管理**をチーム設定に（追加/改名/色/削除・予約ラベル[レビュー/連絡待ち]は保護）。
- **左サイドバー折りたたみ**（56pxアイコンレール、`ts.sidebar.collapsed`永続、各ルートにアイコン）。
- **報告ビュー(#/report)**: 完了/遅延/今日/今週バケット＋コピペ用テキスト＋対話式「報告文ジェネレーター」（手応え等を聞いて噛みやすい箇条書き生成）。
- **ステータスビュー(#/status)**: 共有用の状況ダッシュボード（中立名・上司に見せる用）。
- **ワークスペースをUIから非表示**（実質ワンスペース運用）: フィルタ/列/チップ/投入先選択/集計対象WSを除去。データのproject_idは残し、新規は既定でInbox投入。**かんばんと概要「WS別配分」はWS前提のため対象外＝未対応**（下記§3）。
- **Undo/Redo**（`lib/history.js`）: タスク一覧の編集（担当/分類/重要度/期限/見積/ステータス/完了/一括）をCtrl+Z/Ctrl+Y＋↩↪ボタンで取消/やり直し。削除は対象外（Vikunja仕様で完全復元不可→Undoトースト方式）。**taskform/ガントへの拡張は未**（history singletonは再利用可）。

---

## 2. ユーザーの上司対策（文脈）

「せっかちで状況が見えないと不安な上司」対策。実装済み: 報告ビュー（撃ち返し用）／概要・ホーム・ステータスを共有（A）／中立名ステータス画面（B）。
**未実装(C)**: 「**定期的にSlackで本人に状況を確認→本人が自由文で返信→いい感じの箇条書き報告に整形**」。要件メモ:
- 昼に攻撃が来るので**確認は昼前（例11時）**に寄せる。昼前＋夕方の2回も可。
- 無人送信には **Slack Botトークン or Incoming Webhook が必要**（claude.ai Slack MCPは対話認証＝cron不可）。ユーザーにbot/webhook用意可否＋送り先（DM/チャンネル）＋時刻を確認してから `systemd timer`＋スクリプトで実装する。
- 整形ロジックは report.js の `buildReport` が流用可（純関数化済み）。

---

## 3. ★カンバン：現状と理想の差異（次の主要タスク）

### 現状（`app/views/kanban.js`、API `lib/api.js:44-48`）
- 起動: `projects` からWS一覧→ `ts.kanban.ws` で**単一WSを選択**。WS無し/kanbanビュー無しは「ありません」表示で停止。
- 列 = **Vikunjaのbucket（kanbanビュー固有）**。getViewTasks(pid,vid)で bucket[]+tasks[]。列の追加/改名/削除はあるが**並べ替え/WIP/色なし**。
- カード移動 = `moveTaskToBucket`＝**bucket上書き（ステータス変更ではない）**。
- **スコープ=単一WSのみ・全タスク横断不可・絞り込みなし・列内カード追加なし**。
- WS非表示方針と不整合（WS選択UIが残る・Inbox運用だと窮屈）。

### 理想（ユーザー「超思い通りになる柔軟なカンバン」）と差異
1. **任意軸グルーピング**（ステータス/担当/重要度/プロジェクト=親タスク/分類）←→ 今はbucket固定 🔴
2. **全タスク横断**（WS非依存）←→ 今は単一WS 🔴
3. **ステータス軸カンバン**（未着手/進行中/連絡待ち/完了の自動列、移動でstatus変更）←→ 今はbucket軸 🔴
4. グルーピング軸の**動的切替タブ** 🟡
5. 列の**並べ替え/WIP/色/非表示** 🟡
6. **絞り込み**（プリセットタブ連携：未整理/今日/期限切れ等） 🟡
7. **列内「＋」でカード追加** 🟠

### 推奨実装方針
**Vikunja bucket依存をやめ、フロントで自前グルーピング・カンバンを描画**するのが柔軟で筋が良い：
- `load()` の全タスクを `statusOf(t)` / `humanAssignees(t)[0]` / 重要度 / 親タスク(parentOf) / 分類ラベル で**フロント側グルーピング**。
- 軸切替タブ（status/担当/重要度/プロジェクト/分類）＋選択を localStorage 永続。
- DnDで列移動→その軸のフィールドを更新（status軸ならstatus、担当軸ならassignee付け替え 等）。**Undo/Redo(history.js)に積む**と一貫。
- プリセットタブ（taskMatches）で絞り込み、列内「＋」でインライン追加（taskform/quickaddのensureInbox流用）。
- 既存のVikunja-bucketカンバンは「ステータス軸の一形態」として温存 or 廃止は要相談。
- DnDは gantt/table の作法（HTML5 draggable）を流用。WS非表示方針とも整合（WS選択UI不要に）。
- 工数: 中（レイアウト/DnDは既存流用可。フォーク改修は基本不要＝フロントのみ）。

### 概要(summary)の「WS別配分」も要相談
WS非表示にしたのでWS別配分セクションは浮く。残す/プロジェクト(親タスク)別配分に変える/削除、を要確認。

---

## 3.5 ★メニュー表示/非表示の管理者制御＋各人別出し分け（カンバンの次にやる）

ユーザー要望(2026-06-17): **左ナビ各メニューの表示/非表示を、管理者権限を持つ人が設定できるように。さらにそれを各人別に出し分け**（誰にどのメニューを見せるかを管理者が決める）。

### 要件
- **管理者のみ設定可**（`settings.canEdit` が管理者フラグ。チーム設定保存と同じゲート）。
- **ユーザーごとに表示メニューを設定**（例: 森田=全部 / tanaka=ホーム・タスク一覧・報告のみ）。管理者がメンバー×メニューのマトリクスでON/OFF。
- 設定は**チーム共有＝サーバ保存**。各人がログインすると自分の可視メニューだけナビに出る。

### 実装方針
- **保存先**: exec service の team settings JSON に `menu_visibility: { "<userId>": ["hiddenRouteKey", ...] }` を追加。`lib/store.js` が settings に載せて返す（祝日/容量等と同じ経路）。
- **ナビ描画**: `app/app.js` の shell()/ORDER 走査で、`whoami().id` の非表示集合を除外してナビ生成。隠れたルートへ直URLで来た時はホームへフォールバック。ホーム等の必須メニューは常時表示で固定してよい。
- **設定UI**: チーム設定(settings-team・管理者のみ表示)に「メニュー表示」セクション＝メンバー選択→各ルート(ROUTES.label)のON/OFF。隠し系(fable)や設定自体の扱いに注意。
- 左サイドバー折りたたみ等の**ローカルUI状態とは別物**（こちらはサーバ・全体・管理者制御）。

### 順序
**§3 カンバン再設計の次**に着手。

---

## 4. 残バックログ（小〜中）

- **クイック追加「明日15時」が当日15:00と解釈される**日付パーサ小バグ（`lib/quickadd.js`）。
- **固定サイズ撤廃の他画面スイープ**（ホーム埋め込みは対応済み。対象画面の指定待ち）。
- **Undo/Redoの拡張**（taskform編集・ガント・スマートリストへ。history.js再利用）。
- かんばんが空になりがち（Inbox運用＋bucket無し）→ §3で根本対応。

---

## 5. 運用・進め方メモ

- **オーケストレーション運用契約**（memory `capacity-dashboard-orchestration-contract`）: 画面/関心ごとにサブエージェントへ分割、指示役（メイン）は設計・統合・**ブラウザ検証**に専念。
- 変更は**機能維持を最優先**、各変更後に `node --check`＋**ブラウザ目視（スクショ）**＋consoleエラー0を確認してからコミット。コミットは小さく、メッセージにWHY。
- 共有ロジックは `lib/`（smartlist の `taskMatches`、history、icons、capacity、kinds）。スマートリストとタスク一覧は同じ view 定義/`taskMatches` を共有＝片方直すと両方に効く。
- push: 区切りでユーザーが「push」と言う運用。今セッションの未push分は本ハンドオフと一緒に push する。
