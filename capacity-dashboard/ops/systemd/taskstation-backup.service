[Unit]
Description=TaskStation (Vikunja) daily data backup (dump db+files+env to ~/backups)
After=docker.service
Wants=docker.service

[Service]
Type=oneshot
ExecStart=/bin/bash /home/neo/.local/bin/taskstation-backup.sh
TimeoutStartSec=600
